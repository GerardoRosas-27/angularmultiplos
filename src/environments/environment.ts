// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDijaM0zwVVSLVKo-gA86Fzuk-fvS8Irsw",
    authDomain: "angularmultiplos.firebaseapp.com",
    databaseURL: "https://angularmultiplos.firebaseio.com",
    projectId: "angularmultiplos",
    storageBucket: "angularmultiplos.appspot.com",
    messagingSenderId: "871518419260",
    appId: "1:871518419260:web:1fc98742cc758f3e460639"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
